/**
 * Lab API user. Replaces the image's generated root password so factum2-icinga
 * can authenticate with a known credential.
 */
object ApiUser "factum" {
  password = "factum"
  permissions = [ "*" ]
}
