/**
 * Icinga DB feature. Publishes check results and object state to
 * icingadb-redis; icingadb then writes MariaDB for Icinga Web.
 */
library "icingadb"

object IcingaDB "icingadb" {
  host = "icingadb-redis"
  port = 6379
}
