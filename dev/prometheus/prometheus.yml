global:
  scrape_interval: 1m
  evaluation_interval: 1m

scrape_configs:
  - job_name: snmp
    honor_labels: true
    file_sd_configs:
      - files:
          - /etc/prometheus/targets.json
        refresh_interval: 30s
